@Library('shared-library') _

node('ws') {

env.COMPONENT="frontend"   // this is how variable is declared and this we can call from shared library
env.APPTYPE="angular"
env.TAG_NAME="latest"

docker()

      
}